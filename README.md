# Student Database System
A simple C project to manage student records (Add, Delete, Search,...).
The project is modular (SDB.c, SDB.h, main.c) and I tried to keep the code efficient and memory-friendly.

### How to run:
Compile all .c files and run the executable.
